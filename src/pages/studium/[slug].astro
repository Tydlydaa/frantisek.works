---
// src/pages/studium/[slug].astro
import { getCollection, render } from 'astro:content';
import ProjectDetail from '../../layouts/ProjectDetail.astro';

export async function getStaticPaths() {
  const entries = await getCollection('studium', ({ data }) => !data.draft);
  return entries.map(entry => ({ params: { slug: entry.id }, props: { entry } }));
}

const { entry } = Astro.props;
const { Content } = await render(entry);
---
<ProjectDetail entry={entry} backHref="/#studium" backLabel="Studijní portfolio">
  <Content />
</ProjectDetail>
